// Tailwind v4: theme tokens, darkMode and content detection
// are now configured in globals.css via @theme and @custom-variant.
// This file is kept for tooling compatibility only.
import type { Config } from 'tailwindcss'

const config: Config = {}

export default config
